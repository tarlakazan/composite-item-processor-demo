DROP TABLE members IF EXISTS;

CREATE TABLE members  (  memberId VARCHAR(9) NOT NULL,
                         name VARCHAR(100),
                         surname VARCHAR(100),
                         PRIMARY KEY (memberId)
);








